type Query {
  user(id: ID!): User
  posts: [Post]
}

type User {
  id: ID!
  name: String
  email: String
  friends: [User]
}

type Post {
  id: ID!
  title: String
  content: String
  author: User
}
